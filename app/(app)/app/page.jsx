import RallyApp from '@/components/app/RallyApp'

export const metadata = {
  title: 'Rally — the map',
  description:
    'Report a problem, turn a pin into a volunteer event, and join what neighbors are already organizing.',
}

export default function AppPage() {
  return <RallyApp />
}
